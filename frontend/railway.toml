[build]
builder = "nixpacks"

[deploy]
# VITE_API_URL must be set in Railway (same value as your public backend URL, no trailing slash).
startCommand = "npx --yes serve@14 dist -s -l tcp://0.0.0.0:$PORT"
