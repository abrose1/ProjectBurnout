[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

# Runtime uses Dockerfile CMD (serve). VITE_API_URL must be set for the frontend service
# (build-time for Vite); use the same value as your public backend URL, no trailing slash.
